# Entradas para a referência GLCM

Foram selecionados **37** cortes originais centrais com ambos os pulmões delineados pela máscara fornecida. As imagens em `images/` são links simbólicos para o dataset original; as máscaras em `masks/` são binárias e usam 1 para pulmão. Veja `qc_overview.png` antes de alterar a regra de seleção.
